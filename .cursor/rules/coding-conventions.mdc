---
description: 
globs: src/**/*.svelte,src/**/*.ts,src/**/*.css,src/app.html
alwaysApply: false
---
# Code Conventions Guide

## Official @Docs

When writing code, follow the best practices outlined in their official documentation.

- [Svelte 5](mdc:https:/svelte.dev/docs)
- [Tailwind 4](mdc:https:/tailwindcss.com/docs)
- [Melt UI](mdc:https:/next.melt-ui.com)
- [Lucide Svelte](mdc:https:/lucide.dev/guide/packages/lucide-svelte)
- [Lucide Lab Icons](mdc:https:/github.com/lucide-icons/lucide-lab/tree/main/icons)

## Svelte

This project follows SvelteKit conventions with some specific patterns for static site generation.

### Svelte Version

Any Svelte 4 or earlier code sould be upgraded to the Svelte 5 equivalent. You cannot mix and match Svelte 4 code with Svelte 5 code in the same file.

This includes the following:

- Use runes for declaring reactive state
  ```diff
  -let count = 0;
  +let count = $state(0);
  ```
- Use runes for derivations
  ```diff
  -$: double = count * 2;
  +const double = $derived(count * 2);
  ```
- Use runes for effects
  ```diff
  -$: console.log(double);
  +$effect(() => console.log(double));
  ```
- Use runes for component properties
  ```diff
  -export let optional = 'unset';
  -export let required;
  +let { optional = 'unset', required } = $props();
  ```
- Use properties for events
  ```diff
  -<button on:click={() => count++}>
  +<button onclick={() => count++}>
	clicks: {count}
  </button>
  ```
- Use callback props instead of event dispatchers
  ```diff
   <script lang="ts">
  -  import { createEventDispatcher } from 'svelte';
  -  const dispatch = createEventDispatcher();
  +  let { inflate, deflate } = $props();
     let power = $state(5);
   </script>
  
  -<button onclick={() => dispatch('inflate', power)}>
  +<button onclick={() => inflate(power)}>
     inflate
   </button>
   <button onclick={() => dispatch('deflate', power)}>
   <button onclick={() => deflate(power)}>
     deflate
   </button>
   <button onclick={() => power--}>-</button>
     Pump power: {power}
   <button onclick={() => power++}>+</button>
  ```
- Use render tags instead of slots
  ```diff
   <script>
  +  let { children } = $props();
   </script>
  
  -<slot />
  +{@render children()}
  ```
- Use named props instead of named slots
  ```diff
   <script>
  +  let { header, main, footer } = $props();
   </script>
  
   <header>
  -  <slot name="header" />
  +  {@render header()}
   </header>
  
   <main>
  -  <slot name="main" />
  +  {@render main()}
   </main>
  
   <footer>
  -  <slot name="footer" />
  +  {@render footer()}
   </footer>
  ```
- Use snippets for passing data back up
  ```diff
   // App.svelte
   <script lang="ts">
     import List from './List.svelte';
   </script>
   
  -<List items={['one', 'two', 'three']} let:item>
  +<List items={['one', 'two', 'three']}>
  +  {#snippet item(text)}
     	<span>{text}</span>
  +  {/snippet}
  -  <span slot="empty">No items yet</span>
  +  {#snippet empty()}
     	<span>No items yet</span>
  +  {/snippet}
   </List>
  ```
  ```diff
  // List.svelte
   <script lang="ts">
  -  let { items } = $props();
  +  let { items, item, empty } = $props();
   </script>
   
   {#if items.length}
     <ul>
     	{#each items as entry}
     		<li>
  -  			<slot item={entry} />
  +  			{@render item(entry)}
     		</li>
     	{/each}
     </ul>
   {:else}
  -  <slot name="empty" />
  +  {@render empty?.()}
   {/if}
  ```
- Use functions for manually instantiated components
  ```diff
  +import { mount } from 'svelte';
   import App from './App.svelte'
   
  -const app = new App({ target: document.getElementById("app") });
  +const app = mount(App, { target: document.getElementById("app") });
   
   export default app;
  ```

### Static Adapter Requirements

The project uses `@sveltejs/adapter-static` for GitHub Pages deployment.

- All routes must be prerenderable
- No server-side functionality

## Styling Approach

Follow the [brand-guidelines.mdc](mdc:.cursor/rules/brand-guidelines.mdc) when making layout and style changes.

The Tailwind theme in [app.css](mdc:src/app.css) already has all the colors/typography that you need. Do not add new colors or fonts.

### Icons

Icons from [Lucide Svelte](mdc:https:/lucide.dev/guide/packages/lucide-svelte) are available along with [Lucide Lab Icons](mdc:https:/github.com/lucide-icons/lucide-lab/tree/main/icons) for more niche graphics.

Although you could provide accessible labels to your elements via the `aria-label` attribute, you should generally avoiding this and instead use Tailwind's "visually hidden" utility whenever possible.

```svelte
<div>
  <Phone aria-hidden="true" />
  <span class="sr-only">Phone number</span>
</div>
```

### Components

The project uses [Melt UI](mdc:https:/next.melt-ui.com) for building accessible components that can be styled with [Tailwind](mdc:https:/tailwindcss.com/docs).

### Theming

- Tailwind theme config and global styles in [app.css](mdc:src/app.css)
- Tailwind for utility-first styling
  - Typography plugin for markdown content styling
  - Forms plugin for forms styling

